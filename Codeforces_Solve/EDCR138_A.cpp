#include <bits/stdc++.h>
using namespace std;
int main()
{
    int tc;
    cin >> tc;
    while (tc--)
    {
        int n,m; cin >> n >> m;
        for (int i = 0; i < m; i++){
            int x, y; cin >> x >> y;
        }
        if (n == m) cout << "NO\n";
        else cout << "YES\n";
    }
    return 0;
}
